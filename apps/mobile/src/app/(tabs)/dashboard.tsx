import { PlaceholderScreen } from '@/components/placeholder-screen';

/**
 * Dashboard is parked behind a placeholder until the design is finalized.
 * The previous draft (all sample data) lives in [src/components/dashboard/],
 * ready to be wired back in.
 */
export default function DashboardScreen() {
  return <PlaceholderScreen title="Dashboard" />;
}
