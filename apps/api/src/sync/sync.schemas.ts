// Sync request schemas live in @metabolizm/shared (also used by the mobile
// sync client); re-exported here so controller/service imports stay local.
export {
  syncDiaryQuerySchema,
  type SyncDiaryQuery,
} from "@metabolizm/shared";
