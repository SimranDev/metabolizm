{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src"
  },
  "include": ["src"],
  // Tests stay out of dist/ (and the Docker image); `typecheck` still covers
  // them via tsconfig.json, which has no such exclude.
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
}
